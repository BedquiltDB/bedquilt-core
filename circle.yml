machine:
  timezone:
    UTC
  services:
    - postgresql

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install python-pip build-essential python-dev libpq-dev
    - sudo pip install psycopg2 jinja2
